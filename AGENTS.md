# GameRate Codex 协作规则

本文档用于约束 Codex 在 GameRate 项目中的协作方式，保证项目可以被人工检查、逐步提交和持续维护。

## 1. 基本原则

- 每次修改前先阅读 `docs` 中与任务相关的文档。
- 不要一次性生成大量业务代码。
- 每次任务只完成一个明确目标。
- 修改后必须总结改动文件和主要内容。
- 新增功能要同步更新相关文档。
- 不要提交密钥、token、密码、`.env` 或生产环境配置。
- 不要擅自修改与当前任务无关的文件。
- 不要为了完成任务引入不必要的新依赖。

## 2. 后端协作规则

- 后端保持 Controller、Service、Mapper、Entity、DTO、VO 分层。
- Controller 只负责接收请求、参数校验和返回结果。
- Service 负责业务逻辑。
- Mapper 负责数据库访问。
- Entity 对应数据库表。
- DTO 用于接收请求参数。
- VO 用于返回前端展示数据。
- 新增接口前先查看 `docs/04-接口设计.md`。
- 修改数据库字段前先查看 `docs/03-数据库设计.md`。

## 3. 前端协作规则

- 前端保持页面、组件、API 请求分层。
- 页面放在 `views`。
- 通用组件放在 `components`。
- 接口请求放在 `api`。
- 路由配置放在 `router`。
- 状态管理放在 `stores`。
- 工具函数放在 `utils`。
- 不要在页面中堆积大量接口请求和业务处理。

## 4. 小程序端协作规则

- 小程序端先预留结构，不急于开发完整功能。
- 后续优先复用后端已有 RESTful API。
- 页面结构保持清晰，按首页、列表、详情、个人中心等模块拆分。
- 不要在小程序端先行实现超出 Web 和后端能力范围的复杂功能。

## 5. 文档同步规则

- 新增模块时，先更新 `docs/02-功能模块设计.md`。
- 修改表结构时，同步更新 `docs/03-数据库设计.md` 和 SQL 脚本。
- 新增或调整接口时，同步更新 `docs/04-接口设计.md`。
- 修改开发流程时，同步更新 `docs/05-开发流程.md`。
- 每次 Codex 协作后，可以记录到 `docs/06-Codex任务记录.md`。
- 部署相关变化记录到 `docs/07-部署上线记录.md`。

## 6. 安全规则

- 不提交 `.env`。
- 不提交真实数据库密码。
- 不提交真实 Redis 密码。
- 不提交 JWT 密钥。
- 不提交第三方 API Key。
- 不提交生产环境 `application-prod.yml`。
- 示例配置必须使用占位值。

## 7. 任务边界规则

- 文档任务只修改文档。
- SQL 任务只修改 SQL 和必要文档。
- 后端任务只修改后端和必要文档。
- 前端任务只修改前端和必要文档。
- 小程序任务只修改小程序端和必要文档。
- 如果发现需求超出当前任务范围，应先说明，不直接扩大实现范围。

## 8. 交付规则

每次任务完成后需要说明：

- 新增或修改了哪些文件。
- 每个文件主要做了什么。
- 是否运行了测试或校验。
- 是否有未完成事项。
- 下一步建议做什么。

代码生成后必须能够被人工检查和逐步提交，不追求一次性完成全部系统。
